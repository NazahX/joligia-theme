<section class="hero">
  <div class="section-shell hero__grid">
    <div class="hero__content">
      {% render 'section-heading',
        overline: section.settings.overline,
        title: section.settings.heading,
        text: section.settings.text
      %}

      {% if section.settings.caption != blank %}
        <p class="hero__text">{{ section.settings.caption }}</p>
      {% endif %}

      <div class="button-row">
        {% if section.settings.primary_label != blank %}
          <a class="button button--primary" href="{{ section.settings.primary_link | default: routes.all_products_collection_url }}">
            {{ section.settings.primary_label }}
          </a>
        {% endif %}

        {% if section.settings.secondary_label != blank %}
          <a class="button button--secondary" href="{{ section.settings.secondary_link | default: routes.collections_url }}">
            {{ section.settings.secondary_label }}
          </a>
        {% endif %}
      </div>
    </div>

    <div class="hero__media">
      {% render 'responsive-image',
        image: section.settings.image,
        class: 'hero__image',
        loading: 'eager',
        sizes: '(min-width: 990px) 55vw, 100vw',
        width: 1800
      %}
      {% if section.settings.image_note != blank %}
        <div class="hero__caption">{{ section.settings.image_note }}</div>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero editorial",
  "settings": [
    {
      "type": "text",
      "id": "overline",
      "default": "Joligia",
      "label": "Overline"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "default": "Jewellery for the softer side of midnight.",
      "label": "Heading"
    },
    {
      "type": "richtext",
      "id": "text",
      "default": "<p>Dark romance, sharp detail, and a polished finish that still feels clean, feminine, and expensive.</p>",
      "label": "Text"
    },
    {
      "type": "textarea",
      "id": "caption",
      "default": "Joligia is built around statement pieces with bite: a little dangerous, a little delicate, and styled with restraint.",
      "label": "Caption"
    },
    {
      "type": "text",
      "id": "primary_label",
      "default": "Shop Joligia",
      "label": "Primary button label"
    },
    {
      "type": "url",
      "id": "primary_link",
      "label": "Primary button link"
    },
    {
      "type": "text",
      "id": "secondary_label",
      "default": "Enter the world",
      "label": "Secondary button label"
    },
    {
      "type": "url",
      "id": "secondary_link",
      "label": "Secondary button link"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "image_note",
      "default": "Dark romance",
      "label": "Image note"
    }
  ],
  "presets": [
    {
      "name": "Hero editorial"
    }
  ]
}
{% endschema %}
